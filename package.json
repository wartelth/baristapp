{
  "name": "baristapp",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "app",
    "docs"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "app": "npm start --workspace=app",
    "docs": "npm start --workspace=docs",
    "docs:build": "npm run build --workspace=docs",
    "typecheck": "npm exec --workspace=shared tsc -- --noEmit && npm exec --workspace=server tsc -- --noEmit"
  },
  "devDependencies": {
    "react-refresh": "^0.18.0"
  }
}
